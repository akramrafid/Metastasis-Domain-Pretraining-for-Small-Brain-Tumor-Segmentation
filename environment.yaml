name: brain-mets-domain-pretrain
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pytorch=2.1.2
  - torchvision
  - pytorch-cuda=12.1
  - pip
  - pip:
      - monai[all]==1.3.0
      - hydra-core==1.3.2
      - wandb
      - dvc
      - pytest
      - ruff
      - black
      - nibabel
      - click
      - pandas
      - openpyxl
      - scikit-learn
      - scipy
